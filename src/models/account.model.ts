export class Account {
  name: string;
  email: string;
  accType: string;

  constructor(name: string, email: string, accType: string) {
    this.name = name;
    this.email = email;
    this.accType = accType;
  }

  // TODO:
}
